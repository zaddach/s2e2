PROJECT ( llvm_translator )
CMAKE_MINIMUM_REQUIRED ( VERSION 2.8.8 FATAL_ERROR )

SET ( LIBRARY_NAME "llvm_translator" )
SET ( DEFAULT_BUILD_TYPE "Debug" )
SET ( CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${PROJECT_SOURCE_DIR}/cmake" )

FIND_PACKAGE ( LLVM 3.4 REQUIRED )
FIND_PACKAGE ( QEMU )
FIND_PACKAGE ( GLIB REQUIRED )


INCLUDE_DIRECTORIES ( include ${QEMU_INCLUDE_DIRECTORIES} ${LLVM_INCLUDE_DIRS} ${GLIB_INCLUDE_DIRS} )
ADD_DEFINITIONS ( -DNEED_CPU_H -D__STDC_CONSTANT_MACROS -D__STDC_LIMIT_MACROS )

SET ( LLVM_TRANSLATOR_SRC 
	src/tcg-llvm.cpp
	src/tcg-plugin-main.cpp)
	
FOREACH ( QEMU_TARGET_ARCH ${QEMU_TARGET_ARCHITECTURES} )
	SET ( CURRENT_LIB_NAME ${LIBRARY_NAME}-${QEMU_TARGET_ARCH} )
	ADD_LIBRARY ( ${CURRENT_LIB_NAME} ${LLVM_TRANSLATOR_SRC} )
	SET_PROPERTY ( TARGET ${CURRENT_LIB_NAME} APPEND PROPERTY INCLUDE_DIRECTORIES 
		"${QEMU_SOURCE}/target-${QEMU_TARGET_ARCH}" "${QEMU_BUILD}/${QEMU_TARGET_ARCH}-softmmu" )
	SET_PROPERTY ( TARGET ${CURRENT_LIB_NAME} APPEND PROPERTY COMPILE_FLAGS 
		"-DEMULATION_MODE=\\\"${QEMU_TARGET_ARCH}\\\" -DTARGET_NAME=\\\"${QEMU_TARGET_ARCH}\\\"" ) 
ENDFOREACH ( QEMU_TARGET_ARCH ${QEMU_TARGET_ARCHITECTURES} )